// a JavaScript function determining whether a given number is odd or even.
let number = 88;
function isOddOrEven(number)
{
    if((number%2)==0)
        console.log(` ${number} is even number `);
    else
        console.log(` ${number} is odd number `);
    
}
isOddOrEven(number);

